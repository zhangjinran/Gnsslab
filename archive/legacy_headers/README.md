# Legacy headers

`ConfigData.h` is retained for historical reference only.  It is not included
by any active library or example source, is not part of the `gnss` target, and
is intentionally excluded from the public `include/gnsslab` API.

Do not restore it to the active build without first defining its intended
configuration ownership and consumers.
